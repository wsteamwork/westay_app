import React, { FC } from 'react';
import { View, StyleSheet } from 'react-native';

interface IProps {
  initialProps?: any;
};

const Saved: FC<IProps> = (props) => {
  const { initialProps } = props;

  return (
    <View>

    </View>
  );
};

const styles = StyleSheet.create({

});
Saved.defaultProps = {

}
export default Saved;
